# Retro Reader does not currently use environment variables.
#
# The server port is a hardcoded constant (PORT = 3334) in server.js.
# This file is provided for forward-compatibility / convention only —
# there is nothing you need to fill in to run the app today.
